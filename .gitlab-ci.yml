image: node:6.11.3

pages:
  stage: deploy
  script:
  - npm i
  - npm run build
  - mkdir .public
  - cp -r dist/* .public
  - mv .public public
  artifacts:
    paths:
    - public
    - node_modules
  only:
    - master